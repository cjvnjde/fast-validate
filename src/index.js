import {
  extend,
  validate,
  setLocale,
  setDictionary,
  setValidators,
} from './validator';

export default {
  extend,
  validate,
  setLocale,
  setDictionary,
  setValidators,
}

export {
  extend,
  validate,
  setLocale,
  setDictionary,
  setValidators,
}
